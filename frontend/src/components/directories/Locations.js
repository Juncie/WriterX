import React from 'react';
import Sidebar from '../Sidebar'

function Locations(props) {
    return (
        <div>
            <Sidebar />
        </div>
    );
}

export default Locations;